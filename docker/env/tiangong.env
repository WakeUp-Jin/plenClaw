# 天工（TianGong）容器级配置
# 密钥类配置请放在项目根目录 .env 中，不要放这里

# OpenAI API Key（Codex CLI 需要）
# OPENAI_API_KEY=sk-xxx

# OpenCode 需要的 API Key（也可通过 opencode auth login 配置到 auth.json）
# 支持多种 provider，按需设置对应的环境变量：
# ANTHROPIC_API_KEY=sk-xxx
# OPENAI_API_KEY=sk-xxx（与 Codex 共用即可）
