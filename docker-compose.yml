services:
  pineclaw:
    build: .
    container_name: pineclaw
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PINECLAW_HOME=/root/.pineclaw
    volumes:
      - ~/.pineclaw:/root/.pineclaw
